// Create a copy of this env variable file with naming convention: env.[STAGE].json
// Example: env.dev.json
// Add public key for your registered discord bot as DISCORD_PUBLIC_KEY
// Customize DB_LOCAL_HOST and DB_LOCAL_PORT as needed if you are running a custom local dynamodb for offline testing
{
    "DISCORD_PUBLIC_KEY": "",
    "DB_LOCAL_HOST": "localhost",
    "DB_LOCAL_PORT": 8000,
    "STAGE": "${self:provider.stage}",
    "VWARS_TABLE": "${self:custom.vwarsTableName}"
}